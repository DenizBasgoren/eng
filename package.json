{
  "private": true,
  "name": "eng",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build --no-prerender --no-sw --no-esm --no-inline-css",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-config-synacor": "^3.0.4",
    "preact-cli": "^3.4.3",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "immer": "^7.0.7",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2"
  }
}
